# Supervisor config file
[program:timelapse-ui-app]
directory=/srv/timelapse_ui/
command=/bin/bash -E -c ./start.sh
autostart=true
autorestart=true
stopsignal=INT
stopasgroup=true
killasgroup=true
user=pi
